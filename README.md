Before running the script, you need to ensure it has execute permissions. Use the chmod command to make it executable:

```
chmod +x startup.sh
```

Run consecutively:
```
bash startup.sh
bash run.sh
```
